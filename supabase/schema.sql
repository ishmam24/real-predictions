-- MOVED. The canonical paste-me file is now supabase/setup.sql, which bundles
-- the schema migrations AND the seed data (teams, squads, current gameweek).
--
-- Regenerate it with:
--   node --experimental-strip-types scripts/generate-seed-sql.mjs
--
-- Then paste supabase/setup.sql into the Supabase dashboard → SQL Editor → Run.
